@model IEnumerable<webMVC.Models.hedieuhanh>

@{
    ViewBag.Title = "Index";
    var soluong = 0;
}


<div class="page-title">
    <h3 class="breadcrumb-header">Thông số hệ điều hành</h3>
</div>

<div id="main-wrapper">
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-white">
                <div class="panel-heading clearfix">
                    <a class="btn btn-primary" style="margin-bottom: 10px" href="/admin/hedieuhanhs/Create">Thêm mới thông tin mới</a>
                </div>
                <div class="panel-body">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Tên sản phẩm</th>
                                <th>Hệ điều hành</th>
                                <th>chipset</th>
                                <th>Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th scope="row">@(++soluong)</th>
                                    <td>@Html.DisplayFor(modelItem => item.tensp)</td>
                                    <td>@Html.DisplayFor(modelItem => item.hedieuhanh1)</td>
                                    <td>@Html.DisplayFor(modelItem => item.chipset)</td>
                                    <td>
                                        <a class="btn btn-success" href="/admin/hedieuhanhs/Edit/@item.id">Sửa</a>
                                        <a class="btn btn-info" href="/admin/hedieuhanhs/Details/@item.id">Chi tiết</a>
                                        <a class="btn btn-danger" href="/admin/hedieuhanhs/Delete/@item.id">Xóa</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div><!-- Main Wrapper -->